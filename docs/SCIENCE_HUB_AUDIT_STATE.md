# Science Hub — Audit State (Resumable)

> Read this file FIRST at the start of every session. Then read `SCIENCE_HUB_AUDIT_REPORT.md`.
> Do NOT restart the audit. Continue from "Next Exact Action" at the bottom.

> **2026-09-05 update**: a second, separate full-app audit ran after this file's own 14-phase plan completed (2026-08-21) — 10 parallel specialists (security/database/performance/api/regression/ui/react/typescript/testing/architect) instead of sequential phases, scoped to everything shipped since 2026-08-21. All 38 findings from that round were fixed in the same pass (one left as an open product decision, two confirmed to need no code change) — see the `2026-09-05` entries in `SCIENCE_HUB_AUDIT_REPORT.md` (folded into its existing severity sections) and `DEVELOPMENT_LOG.md`'s Era 15. This file's own phase tracker below still reflects the 2026-08-21 round only — nothing below needed to change as a result of the newer audit, since that one didn't use this file's phase structure.

## AUDIT STATUS

```
Phase 0  — Project Discovery:            COMPLETE
Phase 1  — Auth & Authorization:         COMPLETE
Phase 2  — API Security & Backend:       COMPLETE
Phase 3  — Database & Data Integrity:    COMPLETE
Phase 4  — Business Logic:               COMPLETE
Phase 5  — PDF Generation:               COMPLETE
Phase 6  — File Upload & Storage:        COMPLETE
Phase 7  — Frontend Functionality:       COMPLETE (scoped — see progress detail)
Phase 8  — UI/UX + Responsive:           COMPLETE (scoped — see progress detail)
Phase 9  — Performance:                  COMPLETE
Phase 10 — Scalability:                  COMPLETE
Phase 11 — Error Handling & Resilience:  COMPLETE
Phase 12 — Dependencies & Config:        COMPLETE (all findings fixed, see below)
Phase 13 — SEO / Accessibility:          COMPLETE (scope: Accessibility only, per user decision)
Phase 14 — Testing & Regression:         COMPLETE (scoped — no test database available)
```

## Current Phase
Phases 0, 1, 2, 3, and 12 are all COMPLETE. Phase 4 (Business Logic) not yet started — per explicit user instruction, do not begin Phase 4 work until this file says so, and finish Phase 4 entirely before touching Phase 5. ("jab tak ek phase complete na ho jae next phase pe mat jao")

### Phase 3 progress detail
Full read of `prisma/schema.prisma` (all 20 models) plus cross-checked every FK's actual `onDelete` action against the generated migration SQL (not just the schema file, which can drift from the live DB — this caught the DB-001 gap below).
- **Confirmed correct**: every parent→child "detail" relation (InvoiceItem/Payment/Return→Invoice, PurchaseBillItem/PurchasePayment→PurchaseBill, ReturnItem→Return, RateListItem→RateList, ActivityLog/PasswordResetToken/SectionPermission→User) cascades on delete — matches every route's own "Prisma cascade handles items/payments" comments, verified against actual SQL not just trusted.
- **Confirmed correct**: "referenced by a real business record" relations (Invoice→Customer, PurchaseBill→Vendor) are `RESTRICT` — matches the app's own pre-delete guard checks (customer/vendor delete routes check for referencing invoices/bills first) — DB-level backstop agrees with app-level logic.
- **Confirmed correct**: `StockMovement.productId`/`purchaseBillId` are `SET NULL` — matches the documented "ledger rows survive with a snapshot instead of blocking the delete" design intent (`CLAUDE.md`'s own words), verified in the actual constraint, not just the doc's claim.
- **Verified user's own trigram-index work** (not part of this audit — user confirmed it's already fixed/theirs): `pg_trgm` extension + GIN indexes on Customer/Category/Brand/Product/Invoice/Vendor/PurchaseBill searchable text columns, all present and correctly targeting the fields `/api/search`'s `contains`/ILIKE queries actually use.
- **Finding + fix**: **DB-001** (Medium) — `Vendor` and `PurchaseBill` were the only two soft-deletable models missing `@@index([deletedAt])`, despite both being filtered on that column by every single list/stats query. Added the index to both, wrote migration `20260818130000_add_missing_deletedat_indexes`, applied it to the live Neon DB (user-approved), verified via a direct `pg_indexes` query that both now exist. `prisma generate` + `tsc --noEmit` clean.
- **Informational, not fixed (verified safe)**: **INFO-002** — `InvoiceItem.productId`'s FK is `RESTRICT` while the identically-declared `PurchaseBillItem`/`ReturnItem` product FKs are `SET NULL` (schema-vs-DB drift from before this session) — confirmed harmless since app-level guards block deleting a referenced product either way, so the DB constraint is never actually reached.
- **Informational, deferred by user**: **INFO-003** — DB has an applied migration (`20260811043547_add_custom_name_to_invoice_and_bill`) with no matching local `migration.sql` file. User said skip for now; noted so a future session doesn't rediscover it from scratch.
- **Not separately re-litigated** (already verified thoroughly during Phase 2's route-by-route read, which is the more natural place this evidence surfaced): Serializable-transaction + retry-on-P2034 pattern for every document-number generator and payment-recording route; optimistic-lock (`expectedUpdatedAt`) re-checked atomically via `updateMany` WHERE-match inside the transaction, not just a pre-check; double-delete/double-restore guards via `updateMany` count checks on invoices/purchase-bills/returns; FK-safety pre-checks before every hard-delete in the bin routes (checked both active AND soft-deleted referencing rows).
- **Deferred to Phase 9 (Performance) rather than duplicated here**: `bin/route.ts` and `bin/empty/route.ts` run per-item sequential queries in a `for` loop when purging old products/customers/vendors (N+1-shaped, but bounded by "soft-deleted >30 days ago" volume, which is typically small) — worth a look when Phase 9 runs, not a data-integrity risk.

### Phase 1 progress detail
Reviewed and assessed **solid** (no fix needed, verified not just assumed):
- `src/lib/auth.ts` — bcrypt (cost 12), constant-time dummy-hash comparison for enumeration resistance, per-account (8/15min) + per-IP (30/15min) login rate limiting, DB-error swallowed to generic failure (no distinguishable error), JWT session 8h maxAge, `tokenVersion` invalidation checked every 5 min (bounded staleness, documented tradeoff for perf), section permissions cached in JWT and refreshed every 30s, session cookie `httpOnly`+`sameSite:lax`+`secure` in prod with `__Secure-` prefix in prod. `client()`-triggered `update()` never trusts client-supplied name/email/role — always re-reads DB. No issues found.
- `src/lib/apiAuth.ts` — `requireSession`/`requireAdmin`/`requireWriteAccess`/`requireSectionAccess` all correctly return 401/403 and admin bypasses section checks as documented. Malformed/undefined `sections` array defaults to deny-all (safe default). No issues found.
- `src/lib/rateLimit.ts` — in-memory fixed-window, documented as defense-in-depth only (not distributed-safe) — matches CLAUDE.md's own caveat, not a new finding. `getClientIp()` correctly prefers non-spoofable `x-vercel-forwarded-for`/`x-real-ip` over spoofable `x-forwarded-for`.
- `src/app/api/auth/forgot-password/route.ts` — anti-enumeration done right: identical generic response whether or not account exists, uses `after()` to defer the SMTP send past the response so response-time timing can't distinguish the two branches, escapes name/biz-name into email HTML via `escapeHtml()`, invalidates old unexpired tokens before issuing a new one, per-IP AND per-email rate limits.
- `src/app/api/auth/reset-password/route.ts` — token expiry + single-use (`usedAt`) checked, password length bounds (8-72, matches bcrypt's 72-byte limit), password change wrapped in a transaction with `tokenVersion: increment` (correctly invalidates other sessions), rate-limited by IP.
- `src/app/api/setup/route.ts` — hard-disabled in production, refuses if any user exists (can't be used to take over a live deploy), validates password/email/name bounds.

**Finding recorded**: `src/app/api/auth/find-email/route.ts` — see **AUTH-001** in AUDIT_REPORT.md (Medium). Public, rate-limited endpoint lets anyone enumerate every user's name + masked email via substring search (`contains`, case-insensitive, no role filter) — flagged as a product-decision item, not auto-fixed.

All Phase 1 files reviewed:
- `src/app/api/auth/[...nextauth]/route.ts` — trivial passthrough (`NextAuth(authOptions)`), no issues.
- `src/app/api/admin/users/route.ts` + `[id]/route.ts` — GET/POST/PUT/DELETE all reviewed. Role enum validated server-side (`admin`/`staff`/`manager` only, via `validateUserInput`). Self-edit vs admin-edit branching correct (verified line-by-line): non-admin can only edit self; only admin can change `role`; password change requires `currentPassword`+`bcrypt.compare` when editing self, skipped when admin edits someone else (intentional — admin resets require no old password); email normalized+uniqueness-checked on both create and edit; `tokenVersion` incremented on any password change (invalidates other sessions); DELETE blocks self-delete, blocks deleting the last admin, blocks deleting a user with invoices (FK safety). No issues found — see AUTH-002 (Low, informational) for a style-consistency note only (hand-rolled `getServerSession` instead of the shared `apiAuth.ts` guards; logic itself is correct).
- `src/app/api/admin/profile/route.ts` — own profile+password change, same bcrypt/tokenVersion pattern, correct.
- `src/app/login/page.tsx`, `src/app/forgot-password/page.tsx`, `src/app/find-email/page.tsx`, `src/app/reset-password/page.tsx` — grepped for `localStorage`/`sessionStorage`/`console.log`/`console.error` credential leakage: none found. Login page uses `signIn("credentials", {redirect:false})` + client-side routing, no token handling in JS (NextAuth manages the httpOnly cookie itself). No issues found.
- CSRF reasoning (sameSite=lax cookie blocking cross-site POST-with-cookie) and session-fixation reasoning (JWT strategy has no server-side session store to fixate) were reasoned through but not actively penetration-tested — noted as "Not Verified (reasoning-only)" rather than a gap requiring rework, since both are standard, well-understood properties of this exact configuration (NextAuth v4 JWT + sameSite=lax), not something specific to this app's code that could hide a bug.

**Phase 1 sign-off**: All planned Phase 1 files read and assessed. 1 Medium finding (AUTH-001, find-email enumeration — needs product decision), 1 Low/informational note (AUTH-002). No Critical/High auth issues found in this phase.

## Architecture Map (Phase 0 findings)

- **Stack**: Next.js 16.2.9 (App Router, all pages `"use client"`, no async server components), React 19.2.4, Prisma 5.22 on Neon Postgres, NextAuth v4.24 (Credentials + JWT), Nodemailer/Gmail, client-side PDF gen (jsPDF/html2canvas), Vercel Blob storage, CSS Modules, Vitest + Playwright.
- Full architecture, DB models, API route inventory, cache-tag rules, and "Do Not" conventions are already exhaustively documented in project `CLAUDE.md` (root) — **treat that file as the Phase-0 architecture map**; this audit verifies it rather than re-deriving it from scratch.
- **`middleware.ts`**: default-deny for `/api/**`. Public allowlist: `/api/auth/*`, `/api/setup`, exact `/api/settings/branding`. Verified matches CLAUDE.md description. Uses `getToken()` + checks `token?.id`.
- **`next.config.ts`**: Sets CSP + security headers (X-Frame-Options, nosniff, HSTS, Permissions-Policy). CSP allows `'unsafe-inline'` for script-src/style-src (documented reason: inline theme script + widespread inline `style={{}}`), `'unsafe-eval'` only in dev. `img-src` allowlists `*.public.blob.vercel-storage.com`. Has legacy URL redirects (`/customers` → `/sales/customers` etc.) — permanent 301s, worth checking these don't break old bookmarked links to now-nonexistent paths.
- **tsconfig.json**: `strict: true`. Standard Next.js App Router config.
- 215 `.ts`/`.tsx` files under `src/`, 59 API route files, 45 page files.
- `.env.test.example` present; real `.env.test` presumably gitignored (not confirmed to exist — Pending Task in CLAUDE.md says test DB was never provisioned).

## Dependency Audit (npm audit) — Phase 12, done early because it was a 1-command check

Ran `npm audit` on 2026-08-18. **15 vulnerabilities: 3 critical, 6 high, 5 moderate, 1 low.**

Key findings (see AUDIT_REPORT.md for IDs):
- **`@auth/prisma-adapter`** (`^2.11.2`) is a declared dependency in `package.json` but is **never imported anywhere in `src/`** (grepped, zero matches) — the app uses NextAuth v4's `CredentialsProvider`+JWT, not the Prisma adapter. This unused package pulls in `@auth/core` which has 3 CRITICAL advisories (email homoglyph bypass, `getToken()` uncaught exception on malformed Bearer header, OAuth state/nonce/PKCE cookies not bound to provider). Likely fully removable — dead dependency introducing real vulnerability surface for zero functionality. NEEDS VERIFICATION that `@auth/adapter-prisma`/`PrismaAdapter` isn't referenced in `prisma/seed.ts` or elsewhere outside `src/`.
- `next@16.2.9` has multiple advisories (Turbopack middleware bypass, Server Action DoS/SSRF, cache confusion, Image Optimization SVG DoS) — fix requires `next@16.3.1` (`npm audit fix --force`, outside stated range — needs regression testing given "not the Next.js you know" AGENTS.md warning about breaking changes between versions).
- `nodemailer` has 6 HIGH advisories (SMTP command injection via envelope.size, CRLF injection in EHLO/HELO and List-* headers, jsonTransport bypassing disableFileAccess/disableUrlAccess, TLS cert validation issue, raw-option SSRF/file-read). App's `nodemailer@^7.0.13` — fix is a breaking change to `nodemailer@9.0.5`. **Relevant**: `/api/send-invoice` and `/api/send-rate-list` build the outgoing message — need to check Phase 2 whether any user-controlled input (customer email/name, invoice notes) reaches header/envelope construction in a way any of these CVEs could exploit even on the current version.
- `sharp` (transitive via `next`'s image optimization) — libvips CVEs, DoS via SVG image optimization. Check whether `next/image` is even used anywhere (if not, exposure may be limited to build-time only).
- `postcss`, `undici`, `uuid` (via `exceljs`), `js-yaml`, `brace-expansion`, `dompurify` (moderate, not obviously used directly — check what pulls it in), `cookie` — all transitive, moderate/high, standard "run npm audit fix" fixes exist without breaking changes for most except next/nodemailer/sharp/exceljs's uuid.

Not yet done: reading exact CVE applicability (e.g. does the app's nodemailer usage even construct envelope/headers from untrusted input), deciding fix plan, testing `npm audit fix` in isolation.

## Files Inspected So Far
- `middleware.ts`, `next.config.ts`, `tsconfig.json`, `package.json`, `.gitignore`, `.env.test.example`
- CLAUDE.md / AGENTS.md (project docs, used as pre-existing architecture map)

## Files/Areas Still Pending (large — see phase list)
Everything under Phase 1 onward. Priority order for next sessions:
1. Phase 1: `src/lib/auth.ts`, `src/lib/apiAuth.ts`, `src/app/api/auth/**`, login/forgot-password/reset-password pages, `src/lib/rateLimit.ts`
2. Phase 2: walk all 59 API routes in batches of ~10-15, verify auth guard + input validation + IDOR per route (use CLAUDE.md's API table as checklist, don't assume it's accurate)
3. Phase 3: `prisma/schema.prisma` full read, transaction usage in money/stock mutations
4. Phase 5/6: PDF generation (`generateInvoicePdf.ts`), file upload (`blobStorage.ts`, upload routes)
5. Remaining phases per plan

## Bugs / Vulnerabilities Discovered
See `SCIENCE_HUB_AUDIT_REPORT.md` for full findings with IDs. Summary so far:
- **DEP-001** (High, unused-dependency-with-critical-CVE): `@auth/prisma-adapter` unused, pulls in critical `@auth/core` CVEs. Not yet verified 100% unused outside `src/`.
- **DEP-002..007**: transitive vulnerable packages, see report.

## Phase 2 progress detail (batch 1: invoices — 7/7 files reviewed, COMPLETE for this batch)
Reviewed: `invoices/route.ts` (GET/POST), `invoices/[id]/route.ts` (GET/PUT/DELETE), `invoices/[id]/payment/route.ts`, `invoices/[id]/payment/[paymentId]/route.ts`, `invoices/[id]/returns/route.ts`, `invoices/[id]/returns/[returnId]/route.ts`, `invoices/stats/route.ts`.
All correctly call `requireSession`/`requireWriteAccess`. Server-side recomputation verified for: `isInterState` (via `deriveIsInterState`, never trusts client), transport-charge GST amount, invoice-number generation (Serializable tx + P2034 retry, verified), payment status (both payment routes correctly derive `status` from `paidAmount` inside a Serializable tx re-reading the balance — prevents both overpayment races and the double-payment race). Return/credit-note creation correctly caps against `paidAmount` net of prior returns AND against remaining invoiced-minus-already-returned quantity per product — verified both caps exist and are computed inside the same transaction as the write. Optimistic-lock (`expectedUpdatedAt`) re-checked atomically inside the transaction via `updateMany` WHERE-match, not just as a pre-check — correctly closes the TOCTOU race a naive pre-check would leave open.
**Finding**: **API-001** (High) — the items-omitted "status/notes-only" shortcut branch in `invoices/[id]/route.ts` PUT trusts a client-supplied `status` string with no validation and no tie to `paidAmount`, and Outstanding/Overdue reports filter on that same `status` field — see AUDIT_REPORT.md. Not yet fixed (queued alongside AUTH-001/002 fix-up pass, or fix immediately if user prefers — ask).

## Phase 2 progress detail (batch 2: customers + products — 6 files reviewed)
Reviewed: `customers/route.ts`, `customers/[id]/route.ts`, `products/route.ts`, `products/[id]/route.ts`, `products/[id]/adjust-stock/route.ts`, `products/stats/route.ts`. All correctly guarded (`requireSession`/`requireWriteAccess`), optimistic-locking (`expectedUpdatedAt`) present on customer/product edits, one-off soft-delete pattern for customers verified matching documented convention, duplicate-name checks case-insensitive on products, FK-safety checks before delete (invoice/purchase-bill line-item counts) verified correct on both.
**Finding**: **API-002** (High) — `PUT /api/products/[id]` still allows direct, unaudited `stock` edits via the generic product-edit form, bypassing the `batchAdjustStock()` ledger that `POST /api/products/[id]/adjust-stock` was specifically built to make authoritative (that route's own code comment describes this exact gap). Needs a product decision (make edit-form stock field read-only vs. route it through the ledger too) — not fixed yet, flagged for user like API-001/AUTH-001.

## Phase 2 progress detail (batch 3: vendors + purchase-bills — 9 files reviewed)
Reviewed: `vendors/route.ts`, `vendors/[id]/route.ts`, `purchase-bills/route.ts`, `purchase-bills/[id]/route.ts`, `purchase-bills/[id]/payment/route.ts`, `purchase-bills/upload/route.ts`, `purchase-bills/stats/route.ts`, `purchase-bills/payments/route.ts`, `purchase-bills/payments/stats/route.ts`. All correctly guarded; `payments/route.ts` and `payments/stats/route.ts` correctly use `requireSectionAccess("payments_made")` (noted in their own code comment as a deliberate tightening from a past session that found them only requiring a bare session). File upload route verified genuinely solid: magic-byte validation against declared MIME, 10MB size cap, filename sanitization, blob-URL allowlist on delete. Vendor one-off soft-delete pattern matches the customer pattern exactly. Purchase-bill create/edit: server-recomputes GST/totals from qty×price×rate (never trusts client total), inter-state derived from vendor's own state via `deriveIsInterState`, cancel/un-cancel correctly reverses/reapplies stock, `expectedUpdatedAt` optimistic lock present.
**Finding**: **API-003** (High) — `PUT /api/purchase-bills/[id]` had the same client-trusted-`status` gap as API-001, just narrower (only `"cancelled"` is a legitimate direct value; the code's own comment said so but didn't enforce it). **Fixed** — see report for detail (now only trusts literal `"cancelled"`; anything else recomputes from `paidAmount`).

## Phase 2 progress detail (batch 4: brands + categories + search + bin + settings + admin/permissions + ifsc-lookup + payments + credit-notes + send-invoice + send-rate-list + rate-lists/parse-import + export-xlsx — ~20 files reviewed)
All correctly guarded. Highlights: `admin/permissions/route.ts` correctly validates the section enum and rejects modifying an admin's permissions; `settings/route.ts` correctly uses `encrypt()`/`safeDecrypt()` for Gmail app password + bank account number, admin-only, per-section partial-update pattern verified sound; `settings/ifsc-lookup/[code]/route.ts` regex-validates IFSC format before ever building the outbound URL (no SSRF); `bin/route.ts` and `bin/[type]/[id]/route.ts` FK-safety checks verified thorough (checked both active AND soft-deleted referencing rows, not just active, before allowing a hard delete); `export-xlsx` and `rate-lists/parse-import` both bound their input sizes (row/col caps, 5MB file cap) and validate shapes before handing off to ExcelJS.
**Finding**: **API-004** (High) — `send-invoice`/`send-rate-list` built the email `subject` header from an unsanitized, client-supplied `invoiceNumber`/`title` string with no CR/LF stripping — a real SMTP header-injection gap (the exact class of bug the nodemailer CVEs in DEP-002 describe), reachable by any authenticated write-access user, not just a compromised account. **Fixed** — both routes now reject any `invoiceNumber`/`title` containing `\r`/`\n` or exceeding a sane length cap.

## Phase 2 final batch (admin/activity, settings/logo, settings/branding, rate-lists CRUD, reports, purchase-reports, gst-filing) — all reviewed, Phase 2 now COMPLETE
- `admin/activity/route.ts` + `[id]/route.ts`: admin-only, correct. `DELETE` (clear whole log) logs the clear action itself *after* the delete (correct — logging before would just get wiped by the same deleteMany).
- `settings/logo/route.ts`: same magic-byte + size-cap + admin-only pattern as purchase-bill upload. Solid.
- `settings/branding/route.ts`: confirmed it returns ONLY `{name, tagline, logoUrl}` — deliberately public, nothing sensitive leaks.
- `rate-lists/route.ts` + `[id]/route.ts`: standard CRUD, correctly guarded, bulk-replace-items-in-a-transaction pattern is race-free and appropriate (no stock/ledger side effects to reconcile for a rate list).
- `reports/route.ts`: every dashboard/report type gated by the matching `ProtectedSection` server-side (not just client-side) — verified `requireSectionAccess` calls per `type`, and `combined-dashboard` redacts sales/purchase halves server-side based on the session's actual sections rather than trusting a client-side hide.
- `purchase-reports/route.ts`: same section-gating pattern (`reports_purchases`), including the `stock-ledger` type (whole ledger, not purchase-only, per its own doc note — confirmed intentional, not a bug).
- `gst-filing/route.ts`: combined AND-gate (`requireGstFilingAccess()`) correctly requires BOTH `reports_sales` AND `reports_purchases` (or admin) before merging sales+purchase data into one filing package — good design, prevents a partial/misleading package reaching a half-permissioned user. Noted (not filed as a finding — low confidence, Node's own header-value validation is a backstop): `Content-Disposition` filenames are built from raw `startDate`/`endDate` query strings after only a `new Date(...)` parseability check, not an explicit charset/character allowlist — theoretically fragile if some date-parseable string also contained a header-breaking character, but Node's `http` layer throws on invalid header-value characters (CRLF) before this could do anything, so treated as defense-in-depth already in place rather than a bug to fix.
- **DOC-001** confirmed and filed (Low): CLAUDE.md documents a `DELETE /api/stock-movements?type=stock-ledger` route that does not exist anywhere in the codebase (grepped + globbed, zero matches) — the real (read-only) stock ledger lives at `GET /api/purchase-reports?type=stock-ledger`. No "Clear ledger" UI exists either. Flagged for the documentation-cleanup pass, not fixed now.

## PHASE 2 SIGN-OFF
Every API route in root CLAUDE.md's inventory has been individually opened and checked for auth guard, input validation, IDOR, injection risk, and error handling — except the one route that turned out not to exist (DOC-001). Findings this phase: AUTH-001 (Medium, fixed), AUTH-002 (Low, fixed), API-001 (High, fixed), API-002 (High, fixed per user decision), API-003 (High, fixed), API-004 (High, fixed), DOC-001 (Low, open — documentation pass). Zero Critical findings. Phase 2 is COMPLETE.

## Phase 12 (Dependencies) — closed out 2026-08-18
User asked to fix DOC-001 and "whatever else remains" — used this to close out every open dependency/doc finding from Phase 0's early npm-audit pass:
- Removed unused `@auth/prisma-adapter` (zero real references anywhere in the repo).
- Added `"overrides": {"@auth/core": "0.41.3"}` — `next-auth@4.24.14` pins `@auth/core@0.34.3` as an optional peer dep that npm auto-installs even though nothing uses it; this closed the 3 critical CVEs that removing the adapter alone didn't (they were coming from next-auth's own peer, not the adapter).
- Bumped `next-auth` `4.24.14` → `4.24.15` (patch release fixing 3 next-auth-native advisories: email-normalizer homoglyph bypass, `getToken()` Bearer-header DoS, OAuth state/PKCE binding — none of which are reachable in this app's actual config, since it's `CredentialsProvider`-only with no `EmailProvider`/OAuth, but fixed anyway since the version bump was free/safe).
- Bumped `next` `16.2.9` → `16.3.1` (user-approved, given regression risk) — fixes the middleware-bypass advisory (relevant since `middleware.ts` is this app's whole `/api/**` auth safety net) plus 8 other advisories, and transitively fixes the bundled `postcss`/`sharp` CVEs. Verified: `tsc --noEmit` clean, `npm run build` succeeded with full expected route manifest.
- Bumped `nodemailer` `7.0.13` → `9.0.5` (user-approved) — fixes 6 SMTP/header-injection-class advisories. Verified: `createTransport`/`sendMail` API shape unchanged (sanity-checked via a one-off `node -e` call), `tsc`/`build` clean.
- Ran plain `npm audit fix` for the remaining non-breaking batch (`brace-expansion`, `cookie`, `dompurify`, `js-yaml`, `undici`).
- Deliberately left `uuid`<11.1.1 (via `exceljs`) unfixed — verified `exceljs` only calls `uuid.v4()` with zero arguments; the advisory only affects `v3`/`v5`/`v6` when a caller supplies a custom `buf`, so real exposure is nil, and the suggested "fix" downgrades `exceljs` to `3.4.0` (worse trade for no real gain).
- **Result: `npm audit` went from 15 vulnerabilities (3 critical, 6 high, 5 moderate, 1 low) → 2 moderate (the deliberately-accepted uuid item). Zero critical, zero high remaining.**
- Also fixed **DOC-002** (new, found incidentally while verifying the build): `GET /api/pincode-lookup/[code]` is a real, solid, session-gated+rate-limited+regex-validated route that was simply missing from `CLAUDE.md`'s API table — added it.
- **Follow-up noted, not blocking**: a live smoke test (login → create/edit an invoice → generate a PDF → send-invoice email) against a real dev server/Gmail account is recommended once environment access allows it, since `tsc`+`build` can prove type/compile correctness but not runtime SMTP/Turbopack-middleware behavior. This is a pre-existing gap (no test DB provisioned yet, per CLAUDE.md Pending Tasks), not something this session could close.
- **Confirmed by user**: the `prisma/schema.prisma`/trigram-index migration observed on disk was the user's own deliberate work (already fixed on their end), not something this audit needs to flag or redo. Noted here so Phase 3 doesn't re-flag it as a missing-index gap — verify it's applied/correct rather than re-diagnosing the need for it.

## Fixes Applied
- **AUTH-001** (2026-08-18): `src/app/api/auth/find-email/route.ts` — `contains` → `equals` (case-insensitive) name match. Kills substring enumeration; verified against the page's own UX copy first. `tsc`+`eslint` clean.
- **AUTH-002** (2026-08-18): `src/app/api/admin/users/[id]/route.ts` PUT now uses shared `requireSession()` from `apiAuth.ts`. `src/app/api/admin/profile/route.ts` deliberately NOT touched — its `resolveSessionUser()` email-fallback for pre-`token.id` JWTs would break under `requireSession()`'s strict `session.user.id` check. Caught before shipping; recorded as "Won't Fix by design" in the report, not silently skipped.
- **API-001** (2026-08-18): `src/app/api/invoices/[id]/route.ts` — notes-only PUT branch no longer accepts/writes a client-supplied `status` at all. No caller needed it (grepped every frontend call site). `tsc`+`eslint` clean.
- **API-002** (2026-08-18): Per user decision, made product-edit stock read-only end-to-end: `products/[id]/route.ts` PUT no longer accepts `stock` at all (server-level close, not just UI); `ProductFormFields.tsx` gained a `stockReadOnly` prop (disabled+readOnly input, hint pointing to "Adjust Stock"); `products/[id]/edit/page.tsx` uses it and stopped sending `stock` in its PUT body. New Product page unaffected — opening stock stays editable there (no ledger history to violate on a brand-new product). `tsc`+`eslint` clean on all touched files.
- **API-003** (2026-08-18): `src/app/api/purchase-bills/[id]/route.ts` — `status` now only trusted verbatim when literally `"cancelled"`; any other explicit value is recomputed from `paidAmount`/`total` instead of trusted as-is. `tsc`+`eslint` clean.
- **API-004** (2026-08-18): `send-invoice`/`send-rate-list` routes now reject `invoiceNumber`/`title` containing `\r`/`\n` or exceeding 100/200 chars, before either reaches the email `subject` header. `tsc`+`eslint` clean.

## Tests Performed
- `npm audit` (informational, dependency scan only). Not yet run: `npm run test`, `npm run lint`, `tsc --noEmit`, `npm run build`.

## Last Completed Task
Phase 0 discovery + early dependency scan (Phase 12 partial).

## Phase 4 progress detail — COMPLETE
**Static/library review**: `invoiceCalc.ts`, `roundOff.ts`, `gstLocation.ts`, `purchaseBillForm.ts`, `rateListForm.ts`, `stockMovement.ts` (`batchAdjustStock`) all read line-by-line for edge cases (negative/zero/boundary discount, duplicate-product aggregation, rounding at negative values, FY-boundary math) — all sound. Ran the full Vitest unit suite: **54/54 pass**, 7 test files self-skip (no `.env.test` — confirmed this pre-existing gap from `CLAUDE.md`'s own Pending Tasks is still real; did not attempt to work around it by pointing a test DB at the live database, since that would truncate real data). `documentNumbering.ts`/`rateListForm.ts`/`stockMovement.ts`/`rateListImport.ts` still have no dedicated unit tests — noted for Phase 14.

**Finding (static)**: **BIZ-001** (Medium, fixed) — purchase bills had no duplicate-product-line guard, unlike the equivalent invoice check. Added the identical guard to both purchase-bill routes.

**Live testing** (started `npm run dev`, drove it with a standalone Playwright script since `chromium-cli` wasn't available — logged in as `dev@admin.com` against the real dev database):
- Login flow: clean, no console errors (an initial hydration-mismatch warning turned out to be a testing-script timing artifact — filled the form before React hydration attached handlers — not a real app bug; reproduced cleanly once the script waited for hydration properly).
- **Finding (live-only — static reading in Phase 2 missed this)**: **BIZ-002** (Medium, fixed) — New Invoice/New Purchase Bill defaulted "Transport Charge" to ON, blocking submission. Checked real production data before fixing: only 4/80 invoices (5%) and 1/41 purchase bills (2.4%) actually use it — default was backwards from real usage. Fixed both to default off.
- **Finding (live-only, most severe finding of this whole audit)**: **BIZ-003** (High, fixed) — recording a payment (or a return) on the *same calendar day* an invoice/purchase-bill was created failed with "date cannot be before the invoice/bill date" in all four money-mutating routes (invoice payment create/edit, purchase-bill payment create, returns). Root cause: server compared a bare `"YYYY-MM-DD"` client date against the parent document's full creation *timestamp*; midnight UTC of today is always earlier than a same-day timestamp with a nonzero time-of-day, so this triggered on essentially every real-world "pay on the same day you invoice" scenario — arguably the single most damaging bug found in this entire audit, and one that Phase 2's static route-reading did not catch (the code reads correctly in isolation; it only breaks when actually run). Added `toIstDateStr()` to `validation.ts` and fixed all four call sites. Reproduced the failure live, fixed it, then reproduced success live and confirmed via direct DB query that the payment/return correctly persisted afterward.
- Full end-to-end live verification, each confirmed via a direct database check afterward (not just a UI toast): invoice create (GST/round-off math correct) → payment record (status correctly flips to "partial") → full payment (flips to "paid") → return/credit-note (correctly capped against paid amount on first attempt — confirmed the cap logic itself works — then succeeded once fully paid; stock correctly restored 417→418) → purchase-bill create (stock correctly incremented) → Cancel Bill (stock correctly reversed via a `purchase_cancel` ledger row, 418→417) → bin: create/delete/restore/permanent-delete cycle on a disposable test category, fully verified via DB query (zero rows left after purge).
- **User decision**: the real test invoice/purchase-bill/credit-note created during live testing were left in the dev database as-is (removing them would create the exact GST sequence-gap the bin's own retention design exists to prevent) — not cleanup debris, a deliberate call.

## Interim cleanup (between Phase 3 and Phase 4, user-requested, 2026-08-18)
User asked to remove `feature/custom-invoice-bill-name` (a parked branch, "not needed immediately, revisit later" per its own commit message) and clean up anything of it applied to dev. Findings: never merged into `dev` (verified via git log divergence), so zero source files needed touching — but its one migration (adds `Invoice.customName`/`PurchaseBill.customName`) had been applied directly to the shared dev database independent of git history (this was exactly **INFO-003** from Phase 3). Live DB had 1 non-null value in `Invoice.customName` — flagged to user before acting. With confirmation: wrote+applied migration `20260818140000_drop_unused_custom_name_columns` to drop both columns, deleted the branch locally and on `origin`. `prisma migrate status` now clean ("Database schema is up to date!"). INFO-003 is now resolved/Fixed (updated in the report). No impact on Phase 3's sign-off — this was pre-existing drift from outside this audit's own work, now cleared.

## Phase 5 progress detail — COMPLETE
Live-generated and pixel-inspected actual PDF output (not just checked a blob was produced) for both a single-item and an 18-item real invoice — 18 being the exact threshold where `ITEM_COUNT_FOOTER_SPLIT` first engages.
- **Finding + fix**: **PDF-001** (High) — multi-page invoices duplicated/clipped a line item across the page break, and stamped a doubled/wrong "Page No. X of Y" on every page. Root-caused via temporary diagnostic logging added to the live generator (not guesswork): (1) html2canvas's rendered raster drifts a few px from the pre-capture DOM measurements the split-point math relies on, with no safety margin on row-boundary splits (only the footer had one); (2) the forced-item-cap guard used `start < forcedItemSplitPoint` instead of `pNum === 0`, which combined with fix (1) produced a near-empty extra page; (3) the page-marker erase rectangle was sized to the exact measured element box, too tight to reliably cover html2canvas's differently-rendered text metrics for that same element. Fixed all three; verified by exporting the raw per-page canvas slices as standalone JPEGs (bypassing any PDF-viewer ambiguity) before and after each change, iterating twice until both the item-duplication and marker-doubling were confirmed gone at the pixel level. Regression-checked against the original single-page invoice (near-identical byte count, no visual change). All temporary debug logging removed and reconfirmed clean afterward (byte-identical final output to the last debug run).
- **Finding + fix**: **SEC-003** (Medium) — `PUT /api/settings`'s `logoUrl` field had no validation, unlike every other blob-URL field in the codebase (which use `isPurchaseBillBlobUrl()`/`isLogoBlobUrl()`) — relevant to Phase 5 specifically because `generateInvoicePdf.ts` fetches this URL directly in the browser generating every PDF. Fixed by reusing the existing `isLogoBlobUrl()` helper.
- **Finding + fix**: **PDF-002** (Low) — `generateInvoicePdfBlob()`'s catch-all silently swallowed every failure with no logging, which is exactly why PDF-001 needed temporary instrumentation to diagnose in the first place. Added a `console.error` (behavior/return value unchanged).
- **Verified correct, no fix needed**: **INFO-004** — PDF cache invalidation (`pdfCache.ts`) initially looked like it might have a stale-cache gap on invoice edits; traced further and confirmed the edit page does call `invalidateCachedPdf` correctly on save.

## Post-Phase-5 follow-up (user-reported, 2026-08-18, same day)
User spotted a visible broken-line artifact in the footer border of a downloaded PDF — a direct side effect of the PDF-001 fix's widened marker-erasure rectangle intruding upward into the tfoot box's own background (only ~9px of natural gap existed, vs. 20×SCALE=40px of erasure padding). Chased this through two more iterations: a geometric clamp fix (re-broke the original doubled-text bug on multi-page invoices, confirmed via a raw-canvas debug overlay that the box's true rendered bottom edge doesn't reliably match its live-DOM-measured offset in html2canvas's clone) → final fix via **direct pixel sampling** (scan a text-free canvas column for the actual last non-white pixel, erase everything below that real boundary instead of a geometric estimate). Verified clean across all 4 page/copy combinations of the 18-item invoice plus the single-page invoice, with fresh zoomed screenshots of the footer region for each. All temporary debugging (raw-canvas boundary overlay, in-place pixel markers) removed and reconfirmed byte-identical output afterward. PDF-001 in the report updated with this follow-up rather than filed as a separate finding, since it's the same underlying fix area.

## Phase 5 — closed out (2026-08-19)
Phase 5's original PDF-001/PDF-002 work was made directly to `src/lib/generateInvoicePdf.ts` and was never committed — it sat as uncommitted working-tree changes. Mid-session, while investigating a user-reported regression (item clipping reappeared on an 18-item invoice), a further fix was applied on top, then the user asked to revert *all* Phase-5-era PDF changes to that file. `git checkout -- src/lib/generateInvoicePdf.ts` restored it to the last commit (`81d3454`), which turned out to contain a **different, more mature implementation** than anything this audit's Phase 5 work was built on top of (a computed page-marker band instead of a live DOM element measured per page; natural page-fill instead of a hardcoded `ITEM_COUNT_FOOTER_SPLIT` item-count cap) — the user's own separate rewrite, done independently of this audit. The user has continued actively developing/testing that file themselves since (confirmed via further uncommitted diffs to it during Phase 7) and has explicitly signed off Phase 5 as fully complete based on their own verification of the current PDF pipeline — this audit did not re-verify PDF-001/PDF-002's original failure modes against that current implementation itself. Recorded here so a future session understands *why* Phase 5 is marked complete despite the audit's own fixes never landing in the final code, rather than assuming this pass re-confirmed the current PDF generator.

## Phase 6 progress detail — COMPLETE
Reviewed `src/lib/blobStorage.ts`, `src/app/api/purchase-bills/upload/route.ts`, `src/app/api/settings/logo/route.ts`, attachment-cleanup call sites in `purchase-bills/[id]/route.ts` and `bin/[type]/[id]/route.ts`, and the client-side logo-replace flow in `settings/page.tsx` — then live-tested against the running dev server (not just read) rather than re-trusting Phase 2/5's earlier pass, per the plan below.
- **Finding + fix**: **SEC-004** (High) — `isPurchaseBillBlobUrl()`/`isLogoBlobUrl()` only checked that a URL's host ended in `.public.blob.vercel-storage.com` — a domain suffix shared by every Vercel Blob store on the platform, not unique to this app — so any write-access user could point `attachmentUrl` (or an admin, `logoUrl`) at a completely different store they control, skipping every size/MIME/magic-byte check the real upload routes enforce. Fixed by deriving the app's own store hostname from `BLOB_READ_WRITE_TOKEN` (confirmed the derivation against a real `put()` call's returned URL, not just the token's documented shape) and requiring an exact hostname match. Live-verified: a crafted cross-store `attachmentUrl` that passed the old check is now rejected with `"Invalid attachment URL"`; a real upload→attach→delete round-trip through the app's own store still works unchanged.
- **Live-verified, no fix needed**: magic-byte rejection (a `.txt` relabeled `application/pdf` correctly rejected; a file that genuinely starts with real `%PDF` bytes is accepted, which is the correct/expected boundary of what a magic-byte check can validate), the 10MB size cap (an 11MB file rejected), and filename path-traversal sanitization (`../../../etc/passwd.pdf` correctly flattened to a non-traversing blob key, confirmed via a real upload).
- **Finding + fix**: **SEC-005** (Low) — business-logo replacement cleaned up the old blob via a client-fired, unawaited `DELETE` after the settings `PUT` succeeded, instead of doing it server-side in the same request the way purchase-bill attachment replacement already does. Fixed by moving the cleanup into `PUT /api/settings` itself (mirrors the purchase-bill pattern exactly) and removing the now-redundant client-side calls. Live-verified end-to-end (upload → save → replace → save) with the old blob's deletion confirmed via the Blob SDK's `head()`, not just an HTTP fetch (which can still return 200 from CDN edge cache briefly after a real deletion).
- **Verified correct, no fix needed**: soft-delete of a purchase bill does *not* delete its attachment blob (correct — bin restore needs it to survive); the bin's permanent-delete route does delete it (confirmed in `bin/[type]/[id]/route.ts`); filename sanitization strips path separators before the blob key is built, so the raw (unsanitized) `attachmentName` stored for display is inert since it's only ever rendered as escaped React text, never used in a filesystem path.

## Phase 7 progress detail — COMPLETE (scoped)
Reviewed the shared client-side infrastructure every page depends on: `src/lib/useCache.ts` (the `useFetch`/`mutate`/`patchCache`/`bustCachePrefix` hook), `src/lib/useDirty.ts`, `src/lib/useDebouncedValue.ts`, `src/lib/useCanWrite.ts`, and `src/components/layout/GlobalSearch.tsx` (already carefully abort/race-guarded — no issues found there). Also cleaned up a leftover artifact found in `src/lib/pdfCache.ts` (see below).
- **Finding + fix**: **UX-001** (Low) — `useCanWrite()` returned write-allowed for a manager during the brief window before `useSession()` resolves (`undefined !== "manager"` is `true`). Fixed by returning `false` while `status === "loading"`, before checking `role` — cosmetic-only fix, server-side `requireWriteAccess()` was never the gap here.
- **Cleanup, not a finding**: `src/lib/pdfCache.ts` had an uncommitted `RENDER_VERSION = 10 // TEMP DEBUG bump` left over from this session's earlier PDF debugging (before the `generateInvoicePdf.ts` revert). Explained the mechanism to the user (a cache-key version namespace — bumping it forces every previously-cached PDF to be treated as stale and regenerated fresh, without needing to touch or clear anything else) and, per their choice, kept the versioning mechanism but reset it to a clean `RENDER_VERSION = 1` with the debug comment removed — same effect (still forces one guaranteed fresh regenerate for everyone, so nothing stale from earlier in the session can resurface) without the leftover debug artifact.
- **Scope note**: `git status` showed several pages (`purchases/bills/*`, `sales/invoices/*`) and `generateInvoicePdf.ts` itself under active, uncommitted editing by the user directly (confirmed not this audit's own edits) at the time of this phase — deliberately left those untouched/unreviewed rather than auditing a moving target. A future session should re-scope Phase 7 to cover those pages once they've settled, rather than assuming this pass covered them.

## Phase 8 progress detail — COMPLETE (scoped)
Live-tested at a real 375px mobile viewport (headless browser, logged in as dev@admin.com) across Dashboard, Products list + detail, Invoices list + detail + new, Purchase Bills list + detail + new, Settings, Admin, and Bin. Also grep-swept every `src/components/**/*.module.css` for `@media` count to find candidate gaps, then verified each candidate against a live screenshot/DOM measurement rather than trusting the raw count (several zero-`@media` files turned out fine — see below).
- **Finding + fix**: **UI-001** (Medium) — the status filter tabs' (`All`/`Overdue`/`Unpaid`/`Partial`/`Paid`, shared by Invoices and Purchase Bills lists) mobile `@media` rules in `globals.css` were dead code from a CSS source-order bug (the media blocks were positioned *before* the plain base rules they were meant to override, so the later base rule always won regardless of viewport). Live-measured on a real 375px viewport: tabs overflowed their container by ~22px with "Paid" scrolled off-screen and no visual scroll hint. Fixed by reordering the rules and tightening the 480px breakpoint's padding/font-size — reverified `scrollWidth === clientWidth` (zero overflow) on both the 5-tab and 6-tab (Purchase Bills, includes "Cancelled") variants.
- **Discovered while investigating**: the memory record claiming `StatCardsRow.module.css`/`Pagination.module.css`/`Spinner.module.css` got mobile `@media` fixes on 2026-08-13 is **false** — none of those files have any `@media` rule, and `git log` shows the claimed fix was never committed (same "uncommitted work got lost" pattern as Phase 5's PDF-001/002 — see `project_lost_uncommitted_fixes` memory, now recorded). Live-checked these specific files at 375px anyway: `StatCardsRow` degrades acceptably via its existing `grid-template-columns: repeat(auto-fit, minmax(180px,1fr))` (naturally goes single-column, not broken, just not explicitly shrunk) — no visible breakage found in the pages checked, so not fixed this pass; flagged for a future dedicated sweep rather than blocking Phase 8 on it.
- **Checked, not a bug**: `#invoice-print-area`'s wide GST table (10+ columns) is deliberately wrapped in an `overflow-x:auto` div with `minWidth:700` — the same working pattern documented for Rate Lists in `CLAUDE.md`. Live-verified the scroll actually works (scrolled it programmatically, screenshotted the revealed right-hand columns).
- **Checked, not a bug**: `PdfPreviewModal.module.css` has zero `@media` rules because the component handles mobile via an entirely separate JS-rendered fallback view, not breakpoints.
- **Investigated, user confirmed intentional (not a Phase 8 finding)**: `sales/invoices/new/page.tsx`'s Transport Charge toggle default had reverted from `false` back to `true` (BIZ-002's original fix) — asked the user directly rather than assuming regression, since they'd been actively editing this exact file; they confirmed `true` is now the intended default for invoices specifically (purchase-bills' `new/page.tsx` still correctly defaults `false` and was left alone). Recorded here so a future session doesn't rediscover this as a "BIZ-002 regression" — it's a deliberate product decision.
- **Scope note**: this pass covered the highest-traffic pages and every shared component CSS file's `@media` presence, but did not exhaustively click through every one of the app's ~45 pages at every breakpoint (768px/1024px tablet widths were not separately re-tested this session — DashboardShell's own sidebar/topbar breakpoints were already covered by a prior session's mobile retrofit per `CLAUDE.md`). A future session wanting full Phase 8 closure should spot-check tablet widths and the pages not explicitly listed above (rate lists, credit notes, reports, vendor/customer forms).

## Phase 9 progress detail — COMPLETE
Started from the one item Phase 3 explicitly deferred here (`bin/route.ts`/`bin/empty/route.ts`'s N+1-shaped purge loops), then grep-swept every API route for the same two smells: `for` loops with an `await prisma.*` inside, and sequential (non-`Promise.all`) independent queries. Also checked client bundle size for the app's heaviest dependencies (ExcelJS, html2canvas, jsPDF).
- **Finding + fix**: **PERF-001** (Medium) — the deferred bin-route N+1 queries. Replaced every per-row `count()`/`findMany()` loop (products/customers/vendors purge in both `bin/route.ts` and `bin/empty/route.ts`, plus the "protected reason" computation in `bin/route.ts`) with a single batched `groupBy()` per entity type. Live-verified `GET /api/bin` end-to-end and cross-validated the `groupBy` logic against raw per-row counts on 5 real products (exact match). Did not live-execute `DELETE /api/bin/empty` itself since it's a genuinely destructive bulk-delete — verified via type-check/lint/code-review, same query shape as the live-verified GET fix.
- **Finding + fix**: **PERF-002** (Medium) — `getSalesDashboard`/`getPurchaseDashboard` (`reports/route.ts`) and `getPurchaseSummary` (`purchase-reports/route.ts`) each computed a 12-month breakdown via 12 separate per-month queries fired through `Promise.all` — which doesn't even buy real parallelism in production, since the pooled Neon connection there is `connection_limit=1` (per `CLAUDE.md`), so "concurrent" queries just queue through one connection anyway. Replaced each with one `findMany()` over the full range + JS grouping. Live-verified all three endpoints; cross-checked correctness by confirming the current month's total matched an independently-computed value from an unrelated function, and by manually re-deriving one month's total from the raw data.
- **Finding + fix**: **PERF-003** (Low) — `getReportSummary()` (`src/lib/db.ts`) awaited 6 independent queries sequentially instead of via `Promise.all`. Fixed; live-verified `GET /api/reports?type=summary`.
- **Checked, not a bug**: `ExcelJS` is only ever imported from API routes (server-only). `html2canvas`/`jsPDF` are already dynamically imported in `generateInvoicePdf.ts` — live-verified via a real browser network trace that visiting `/dashboard` does not load the chunk containing them.
- **Checked, not a bug**: every other report/aggregation function reviewed (`getReportOutstanding`, `getReportStock`, `getPurchaseOutstanding`, `getPurchaseByCategory`, `getStockLedger`, `getGstSummary`, `getCombinedDashboard`, `gstFiling.ts`'s report builder) was already either a single query or a properly-batched `Promise.all` of independent queries — no further N+1 or unnecessary-serialization patterns found in the API routes. `setup/route.ts`'s sequential seed-data creates were reviewed and left alone — it's the one-time, admin-only, pre-first-user bootstrap route with a small fixed dataset, not a real-world scaling concern.
- **Scope note**: this pass covered database query patterns (N+1, unnecessary serialization) and top-level bundle-splitting — it did not do a full Lighthouse/Core-Web-Vitals-style audit, image optimization review, or a line-by-line React re-render/memoization audit. A future session wanting deeper Phase 9 coverage could look at those angles specifically.

## Phase 10 progress detail — COMPLETE
Focused on unbounded-growth query patterns — result sets or computation that scale with total data volume (customers, invoices, products) rather than staying bounded regardless of how large the business gets.
- **Finding + fix**: **SCALE-001** (Medium) — `getSalesDashboard`/`getPurchaseDashboard`'s "top 5 customers/vendors" fetched *every* customer/vendor with *every* one of their invoices/bills and sorted in JS. Replaced with a DB-side `groupBy` + `orderBy` on the aggregated sum + `take: 5`, then a tiny follow-up query for just those 5 names. Preserved the original soft-delete exclusion via a relation filter. Live-verified both endpoints return 5 correctly-sorted, correctly-named entries.
- **Finding + fix**: **SCALE-002** (Medium) — several dashboard/summary functions fetched every unpaid/partial invoice or bill (or every product) to sum/filter in JS, instead of using the real generated `balanceDue`/`isLowStock` Postgres columns directly via `aggregate()`/`count()`. Also collapsed a `getCombinedDashboard` low-stock count that wastefully ran a `count()` whose result was discarded before doing the real work via a separate findMany+filter. Live-verified: the same outstanding-balance figure came back identical across 3 independently-refactored code paths, and matched a from-scratch manual reduce over the live data to the last paisa.
- **Checked, not a bug**: `search/route.ts` is already well-bounded (`take: 5` per entity group) and backed by the `pg_trgm` GIN indexes confirmed in Phase 3 — scales fine as data grows. All `/stats` routes (`credit-notes`, `payments`, `purchase-bills/payments`, `products`, `invoices`, `purchase-bills`) already use `aggregate()`/`count()` correctly — `getProductStats` was already using the `isLowStock: true` filter pattern that SCALE-002's fix now applies consistently elsewhere. Rate limiting's in-memory, non-distributed nature was already reviewed and explicitly accepted in `CLAUDE.md` (a Redis/Upstash-backed version was tried and reverted) — not re-litigated here.
- **Scope note**: this pass focused on query/data-volume scalability, not infrastructure-level scaling (serverless concurrency limits, DB connection pool sizing under load, CDN/caching strategy) — a future session wanting deeper Phase 10 coverage could load-test the pooled Neon connection (`connection_limit=1`) under concurrent requests specifically.

## Phase 11 progress detail — COMPLETE
Focused on unhandled-failure resilience: what happens when something breaks unexpectedly, both client-side (render errors) and server-side (external service failures, silent-catch gaps).
- **Finding + fix**: **RESIL-001** (High) — the app had **no** `error.tsx`/`global-error.tsx`/`not-found.tsx` anywhere, despite every page being a client component. Added `(dashboard)/error.tsx` (sits beside, not inside, the layout, so `DashboardShell`'s sidebar/topbar survive a page-level crash) and a self-contained `global-error.tsx` for a root-layout-level failure. Live-verified by temporarily adding a page that unconditionally threw: confirmed via console output ("handled by the `<ErrorBoundaryHandler>` error boundary"), confirmed the new `console.error` fired, and screenshotted the result — sidebar/topbar fully intact, clean recovery UI with working "Try again"/"Go to Dashboard" buttons. Test page removed afterward.
- **Finding + fix**: **RESIL-002** (Low) — `ifsc-lookup` treated any non-2xx from the upstream Razorpay directory (including a real outage) as "IFSC code not found", unlike the already-correct `pincode-lookup` route. Fixed to only treat a genuine `404` as "not found"; other non-2xx now correctly reports "could not reach the service" (502).
- **Finding + fix**: **RESIL-003** (Low) — `send-invoice`/`send-rate-list`'s nodemailer transports had no explicit timeout (defaults allow up to a 10-minute hang) on synchronous request/response routes. Added explicit 10-15s timeouts so a stuck SMTP connection fails fast into the existing `catch` block instead of hanging until the platform's own function-timeout kills it.
- **Cleanup**: added `console.error` to `logActivity()`'s and `pdfCache.ts`'s empty catch blocks (both correctly never throw, by design, but previously left zero trace on failure) — consistent with PDF-002's earlier precedent. Given this exact session's own earlier saga with stale cached PDFs, a silently-failing cache-invalidation was judged worth surfacing.
- **Checked, not a bug**: spot-checked dozens of client-side page `catch {}` blocks across `bin`, `admin`, `reports`, `purchases`, `sales` pages — all correctly show an error toast, none are actually silent. `pdfIframeGenerator.ts`'s empty catch around a DOM `removeChild` left as-is (an already-removed node is an expected, uninteresting race, not worth logging).
- **Scope note**: this pass covered error boundaries and a handful of concrete external-service/silent-catch gaps found by direct inspection — it did not do an exhaustive line-by-line review of every try/catch in the codebase, nor test actual database-connection-loss behavior mid-transaction (Phase 2/3's Serializable+P2034-retry pattern for money/stock mutations was already verified in earlier phases and not re-litigated here).

## Phase 13 progress detail — COMPLETE (scope: Accessibility only, per user decision)
User's call (agreed with the reasoning): this app is 100% internal/auth-gated, so traditional SEO work (sitemap, OpenGraph, structured data, keyword meta) has no audience and was skipped entirely. Kept one narrow SEO-adjacent item — a `robots.txt` disallowing all crawling, as defense-in-depth against a stray crawler indexing the public login/forgot-password pages. Accessibility was treated as the real scope of this phase, since it affects the app's own staff/admin users regardless of public/private status.
- **Finding + fix**: **A11Y-001** (Medium) — no "skip to main content" link existed; a keyboard user had to tab through the entire ~19-link sidebar nav on every page. Added one, live-verified (first Tab focuses it, Enter navigates to `#main-content`).
- **Finding + fix**: **A11Y-002** (Low) — a dedicated sweep (via a background Explore agent) for icon-only buttons across all of `src/` found exactly 3 gaps, all with only a `title` (or nothing) instead of `aria-label` — the rest of the app's icon-only buttons already had this correctly. Fixed all 3 plus one in `PdfPreviewModal.tsx` found directly.
- **Finding + fix**: **A11Y-003** (Medium) — `FormField` correctly wired `htmlFor`/`id` (label↔control) but never wired `aria-describedby` (control↔error/hint text), despite `CLAUDE.md`'s own stated standard calling for both. Fixed and live-verified against a real form's validation errors.
- **Finding + fix (upgraded to Medium)**: **A11Y-004** — originally found one marginal failure (`--c-text-3` on `--c-bg-sub`, light mode, 4.34:1 vs the 4.5:1 AA threshold). User then asked to also give the active filter tab a solid blue background — checking that surfaced a much worse **pre-existing, previously-unknown** bug: the sidebar's `.navLinkActive` already used `var(--c-blue)` + white text with zero dark-mode adjustment, and dark mode's `--c-blue` is a pale/white-mixed color — white text on it is only 2.72:1. Fixed both (inactive tab darkened in light mode only; both active-blue components switched to `--c-blue-h`, a darker solid blue, in dark mode specifically) — also caught and fixed a real bug introduced while implementing this: a bare `.dark X` selector inside a CSS Module file (`DashboardShell.module.css`) silently matches nothing since CSS Modules hash bare class selectors by default; needed `:global(.dark)` to actually reach the real global class, per that file's own existing convention. Live-verified all 4 theme×component combinations via real rendered `getComputedStyle` colors, not just source CSS or manual math.
- **Also added**: `src/app/robots.ts` (Next.js App Router convention) — disallows all crawling. Live-verified `/robots.txt` serves correctly.
- **Checked, already solid — no fix needed**: focus-visible styles (Button/Input/Select all have proper custom replacements for the suppressed native outline), semantic landmarks (`<aside>`/`<nav>`/`<main>` already used correctly in `DashboardShell`), image alt text (every `<img>` in the app already has one), and `Modal.tsx`'s dialog semantics (`role="dialog"`, `aria-modal`, focus trap, Escape handling, focus restoration on close — all already correctly implemented).
- **Scope note**: this pass covered the accessibility fundamentals most likely to have app-wide impact (skip link, icon-button labels, form error announcement, color contrast) via targeted checks and one broader automated sweep — it was not an exhaustive page-by-page WCAG audit (e.g. did not check every page's heading hierarchy individually, or run an automated tool like axe-core/Lighthouse across all ~45 pages). A future session wanting deeper Phase 13 coverage could run an automated accessibility scanner across the full page set.

## Phase 14 progress detail — COMPLETE (scoped — no test database available)
- **Attempted, declined by user**: asked whether to test directly against the live dev database. Explained the real risk first: `tests/api/**` truncates every table before each test, and `vitest.config.mts` has a hard safeguard specifically refusing to start if `TEST_DATABASE_URL` equals the real `DATABASE_URL`, precisely to prevent this. User's first read of "dev db pe hi test karo" turned out to mean choosing the safe local-DB alternative once the risk was explained, not actually wiping the dev database.
- **Attempted, blocked by environment**: checked for local Postgres and Docker to stand up a disposable local test DB instead of a cloud one — neither is installed on this machine. Asked the user to choose between provisioning a Neon branch (needs their account), installing Docker Desktop, or skipping DB-backed test execution — user chose to skip it for this session.
- **What was done instead**: ran `npm run test` (54 unit tests pass, 56 API tests self-skip — matches documented behavior exactly). Read every `tests/api/*.test.ts` and `tests/e2e/*.spec.ts` file directly and judged their quality independent of runnability: genuinely good (real route handlers, no mocking of what's under test, explicit regression tests referencing the specific past bugs they guard, 403/401 cases covered, E2E specs correctly avoid `.selectOption()` on the app's custom non-native `Select`).
- **Finding + fix**: **TEST-001** (Low) — `CLAUDE.md`'s test-coverage note claimed `purchase-bills`/`payments`/`returns`/`credit-notes`/`rate-lists`/bin-restore had "no tests yet" — false; `purchase-bills.test.ts`, `purchase-bill-payments.test.ts`, and `returns.test.ts` all already have substantial real coverage (148-280 lines each). Rewrote the note with the actual current per-route state.
- **Closed part of the gap immediately, since it didn't need the blocked test DB**: `documentNumbering.ts` and `rateListForm.ts` are pure functions (no Prisma import) — added `tests/unit/documentNumbering.test.ts` (29 tests: prefix derivation, FY boundary math incl. century wraparound, all 3 number-format layouts' render/matcher/dbFilter, the "9 vs 10" lexicographic-sort trap `findMaxSequence` exists to avoid, override precedence) and `tests/unit/rateListForm.test.ts` (16 tests: discount clamping both directions, net-rate bypass, rounding, full validation-error paths). Both run and pass for real (99/99 unit tests now, up from 54) — not just written speculatively. `stockMovement.ts` was left alone — it takes a Prisma transaction client (`tx`) as a real parameter, so testing it meaningfully needs the same blocked database, not just pure-function logic. `rateListImport.ts` (also pure) was identified as the next candidate but not reached this session.
- **Still genuinely untested** (confirmed by reading, not guessing): `brands`/`categories`/`products`, `rate-lists`, the `credit-notes` list route itself, the sales `payments` sub-route on invoices, `settings`, `admin/*`, `search`, `send-invoice`/`send-rate-list`, `gst-filing`, `export-xlsx`, `bin`'s permanent-delete/`empty` (all DB-integration-shaped, blocked without a test database), plus `rateListImport.ts` and `stockMovement.ts` unit tests.
- **Residual risk, explicitly not resolved this session**: none of `tests/api/**`/`tests/e2e/**` has ever actually executed against a real database — only confirmed to compile and self-skip. A logic bug inside an untested code path wouldn't be caught by this. Also: this whole session's fixes (PERF-*, SCALE-*, RESIL-*, A11Y-*, SEC-*, UI-*) shipped without new regression tests, since none could be verified to even run without the same missing test database — a future session with DB access should both provision it and backfill regression tests for this session's fixes.

## Next Exact Action
Phase 14 is complete for the scope covered above — **this was the final phase in the original 14-phase audit plan.** All phases (0 through 14) are now COMPLETE, at whatever scope each phase's own notes describe (several — 5, 7, 8, 13, 14 — are explicitly scoped/partial rather than exhaustive; see each phase's own progress detail above before assuming full coverage). The two biggest open threads for a future session: (1) provision a real test database and actually run `tests/api/**`/`tests/e2e/**` for the first time, backfilling regression tests for this session's fixes; (2) revisit Phase 7's skipped invoice/purchase-bill pages once your own active edits there settle.

## Last Updated
2026-08-21 (session 3 — Phase 14 (Testing & Regression) COMPLETE, final phase of the original plan. TEST-001 (Low, fixed) — stale CLAUDE.md test-coverage claim corrected. DB-backed tests (`tests/api/**`/`tests/e2e/**`) stayed blocked (no local Postgres/Docker, cloud branch needs the user's account, and testing against the live dev DB was correctly declined — it would have destroyed real data via the suite's own table-truncation-per-test design) — but added and **ran for real** two new unit-test files needing no DB at all: `documentNumbering.test.ts` (29 tests) and `rateListForm.test.ts` (16 tests) — unit suite now 99/99 passing, up from 54/54. All 14 audit phases now COMPLETE. Phase 13 (Accessibility) COMPLETE: A11Y-001 (Medium) skip-link, A11Y-002 (Low) icon-button labels, A11Y-003 (Medium) aria-describedby, A11Y-004 (Medium) color contrast + a previously-unknown sidebar dark-mode bug + a CSS-Modules `:global()` bug, all fixed + live-verified. No next phase — audit plan complete; see "Next Exact Action" above for the two biggest remaining threads.)
